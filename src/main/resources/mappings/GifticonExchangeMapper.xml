<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hungpick.dao.IGifticonExchangeDao">

	<!-- 기프티콘 교환 : 보유 포인트 수정 -->
   	<update id="exchangeGifticon">
		UPDATE MEMBERS SET HOLD_POINT = #{holdPoint} WHERE GIFTICON_CODE = #{gifticonCode} AND BRAND_CODE = #{brandCode} AND MENU_CODE = #{menuCode}  
	</update>
	
</mapper>
