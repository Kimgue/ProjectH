<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hungpick.dao.IGifticonDao">


	<select id="select" parameterType="string" resultType="gift">
		SELECT * 
		FROM 
		GIFTICON
		WHERE GIFTICON_CODE = #{ gifticonCode } 
		AND BRAND_CODE = #{ brandCode } AND MENU_CODE =#{ menuCode }
	</select>
	
	<select id="selectgift" resultType="gift">
		SELECT * FROM GIFTICON 
	</select>
	
   <insert id="insert" parameterType="gift">
	   INSERT  INTO GIFTICON       
	                 VALUES(   #{ gifticonCode } , #{brandCode} , #{ menuCode } , #{ gifticonName },  #{ gifticonImg } ,  #{ gifticonPrice} )
                
   </insert>
	
	<update id="update" parameterType="gift">
		UPDATE GIFTICON SET #{ gifticonName },  #{ gifticonImg } ,  #{ gifticonPrice}  WHERE GIFTICON_CODE = #{ gifticonCode } AND BRAND_CODE = #{ brandCode } AND MENU_CODE =#{ menuCode }  
	</update>

	<delete id="delete">
		DELETE FROM GIFTICON WHERE GIFTICON_CODE = #{ gifticonCode } AND BRAND_CODE = #{ brandCode } AND MENU_CODE =#{ menuCode }
	</delete>
	
	
</mapper>
